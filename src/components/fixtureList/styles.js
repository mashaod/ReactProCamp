function styles(theme) {
    return {
        root: {
            margin: 0,
            padding: theme.spacing(2)
        },
        eventIcon: {
            width: "30px",
            height: "30px",
        }
    }
}

export default styles;